export interface IButton {
  text: string;
  onClick: () => void;
  color: string;
  background: string;
}

export interface IText {
  text: string;
  fontWeight: string;
  fontSize: string;
  color: string;
}
